# Core LangGraph dependencies
langgraph>=0.6.6
langchain-core>=0.3.0
langchain-openai>=0.2.0

# For environment variables
python-dotenv>=1.0.0

# LangGraph checkpoint (for conversation history)
langgraph-checkpoint>=2.0.0
